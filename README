Instrucciones:

- Crear el archivo "config.ini" en la carpeta cloudinator/assets a partir del archivo "config-example.ini"

- Modificar el archivo "config.ini" y poner los datos de la base de datos

- Recuerde configurar su servidor httpd para que no entregue archivos *.ini (Seguridad)
	Esto se hace agregando las siguientes lineas en el archivo "/etc/httpd/conf/httpd.conf":
		<Files *.ini>  
			Order deny,allow
			Deny from all
		</Files>

- Ejecute /cloudinator/editor/installdb.php

- Ejecute www.example.com/cloudinator/DB/upgrade.php